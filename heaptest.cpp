#include "minheap.h"
#include <iostream>

using namespace std;

int main(){

	/*
	MinHeap heap;

	pair<string, int> p("Daniel", 1);
	pair<string, int> q("Alex", 1);
	pair<string, int> r("Hello", 1);
	pair<string, int> s("Chumba", 1);
	pair<string, int> t("Wumba", 1);
	pair<string, int> u("Chatanooski", 1);
	pair<string, int> v("Benis", 1);
	pair<string, int> test("test", 1);
	pair<string, int> help("asdf", 1);

	heap.insert(p);
	heap.insert(q);
	heap.insert(r);
	heap.insert(s);
	heap.insert(t);
	heap.insert(u);
	heap.insert(v);
	heap.insert(test);
	heap.insert(help);
	heap.print();
	*/
	return 0;

}
